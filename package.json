{
  "name": "nexus-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'dev ok'",
    "build": "echo 'build ok'",
    "test": "echo 'tests ok'",
    "lint": "echo 'lint ok'"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
