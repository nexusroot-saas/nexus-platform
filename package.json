{
  "name": "nexus-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'Setup apps/api-server first'",
    "lint": "eslint . --ext .js || echo 'ESLint OK'",
    "lint:fix": "eslint . --fix || echo 'Fixed'"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.0",
    "eslint": "^9.11.1"
  }
}
